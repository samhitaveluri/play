import React from 'react';
import { Link } from 'react-router-dom';

const HomePage = () => (
  <div className="jumbotron">
    <h1>Podcast Administartion</h1>
    <p>React, Redux and React Router for responsive web apps</p>
  </div>
);

export default HomePage;
